// src/components/Card.js
import React from 'react';

const Card = ({ title, description }) => {
  return (
    <div className="card">
      <h3>hello</h3>
      <p>{description}</p>
    </div>
  );
};

export default Card;